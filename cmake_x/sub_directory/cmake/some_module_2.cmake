# STATUS is optional in message(), However, if ommited, the message will be
# printed without the double dashes prepended, so it looks ugly in the std out.
#message("some_module_2 is running")
message(STATUS "some_module_2 is running")
