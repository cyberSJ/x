#note: the compile command can be derived by looking at the 
# Magick++ API compilation procedure from the ImageMagick website.
# However, the MagickWand API compilation procedure doesn't work.
# Only the Magick++ API compilation procedure worked.
CC = gcc
INC = -I /home/sung/Downloads/ImageMagick-6.8.7-5/
LIB = -L /usr/local/bin

all:
	$(CC) -g -Wall `MagickWand-config --cflags --cppflags` imageMagickX.c `MagickWand-config --ldflags --libs` -o imageMagickX
#	$(CC) -g -Wall -o imageMagickX `pkg-config --cflags --libs Wand` imageMagickX.c -lMagickWand
